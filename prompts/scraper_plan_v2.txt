You are planning a robust job-board scraper for {company}.
Careers entry URL: {url}

Return exactly one JSON object matching this schema:
{schema}

Priority rules:
1. If NETWORK EVIDENCE contains a JSON endpoint whose response carries job records, emit mode
   "api". Prefer this over DOM selectors because the endpoint is the portal's stable data source.
2. Otherwise emit mode "dom" using the captured DOM. Choose static unless rendering or browser-only
   pagination is required.
3. Use at most the provided declarative navigate actions when listings are not on the entry page.
4. Never invent credentials, authentication headers, executable code, or an unrelated host.
5. Use only accept/content-type request headers. Keep pagination bounded.

RETRY FEEDBACK AND PRIOR PLANS
{retry_feedback}

NAVIGATION PATH
{navigation_path}

REPEATED LINK PATTERNS
{link_patterns}

NETWORK EVIDENCE
{network_summary}

DISTILLED DOM
{dom}
